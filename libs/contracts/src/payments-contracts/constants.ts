export enum Currency {
	USD = "usd",
	EUR = "eur",
	BYN = "byn",
	RUB = "rub",
}

export enum PaymentInterval {
	DAY = "day",
	WEEK = "week",
	MONTH = "month",
}

export enum TransactionStatusEnum {
	PENDING = "PENDING",
	SUCCESS = "SUCCESS",
	FAILED = "FAILED",
	EXPIRED = "EXPIRED",
}
