apiVersion: apps/v1
kind: Deployment
metadata:
  name: DEPLOYMENT_NAME
  namespace: NAMESPACE
  labels:
    app: PROJECT
spec:
  selector:
    matchLabels:
      project: PROJECT
  template:
    metadata:
      namespace: NAMESPACE
      labels:
        project: PROJECT
    spec:
      containers:
         - name: PROJECT
           image: REGISTRY_HOSTNAME/PROJECT:TAG_VERSION
           ports:
             - containerPort: PORT_CONTAINER

           env:
             - name: AUTH_TCP_HOST
               valueFrom:
                 secretKeyRef:
                   name: ulens-backend-auth-production-config-secret
                   key: AUTH_TCP_HOST
             - name: AUTH_POSTGRES_URL
               valueFrom:
                 secretKeyRef:
                   name: ulens-backend-auth-production-config-secret
                   key: AUTH_POSTGRES_URL
             - name: REFRESH_SECRET_KEY
               valueFrom:
                 secretKeyRef:
                   name: ulens-backend-auth-production-config-secret
                   key: REFRESH_SECRET_KEY
             - name: REFRESH_EXPIRES_IN
               valueFrom:
                 secretKeyRef:
                   name: ulens-backend-auth-production-config-secret
                   key: REFRESH_EXPIRES_IN
             - name: AUTH_TCP_PORT
               valueFrom:
                 secretKeyRef:
                   name: ulens-backend-auth-production-config-secret
                   key: AUTH_TCP_PORT