export enum FilterByStatus {
	BLOCKED = "BLOCKED",
	NOT_BLOCKED = "NOT_BLOCKED",
	ALL = "ALL",
}

export enum SortabeFieldsForUsers {
	CREATED_AT = "createdAt",
	USER_NAME = "userName",
	EMAIL = "email",
	IS_BLOCKED = "isBlocked",
}

export enum FollowType {
	FOLLOW = "FOLLOW",
	UNFOLLOW = "UNFOLLOW",
}
